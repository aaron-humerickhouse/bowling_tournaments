<%= form_with(model: alley) do |form| %>
  <% if alley.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alley.errors.count, "error") %> prohibited this alley from being saved:</h2>

      <ul>
        <% alley.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, class: 'label' %>
    <div class="control">
      <%= form.text_field :name, class: 'input' %>
    </div>
  </div>

  <h4 class="subtitle is-4">Address</h4>
  <%= form.fields_for :address_attributes do |address| %>
    <div class="field">
      <%= form.label :street_address, class: 'label' %>
      <div class="control">
        <%= form.text_field :street_address, class: 'input' %>
      </div>
    </div>

    <div class="field">
      <%= form.label :secondary_address, class: 'label' %>
      <div class="control">
        <%= form.text_field :secondary_address, class: 'input' %>
      </div>
    </div>

    <div class="field">
      <%= form.label :city, class: 'label' %>
      <div class="control">
        <%= form.text_field :city, class: 'input' %>
      </div>
    </div>

    <div class="field">
      <%= form.label :state, class: 'label' %>
      <div class="control">
        <%= form.text_field :state, class: 'input' %>
      </div>
    </div>

    <div class="field">
      <%= form.label :zip_code, class: 'label' %>
      <div class="control">
        <%= form.text_field :zip_code, class: 'input' %>
      </div>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit class: "button is-primary" %>
  </div>
<% end %>
